services:
  db:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: museum
      POSTGRES_PASSWORD: museum
      POSTGRES_DB: museum_compass
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
