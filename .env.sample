# PostgreSQL 接続文字列
DATABASE_URL="postgresql://museum:museum@localhost:5432/museum_compass?schema=public"
# PostgreSQL 直接接続（本番のみ。Vercel が自動設定。ローカルでは不要）
# DATABASE_URL_UNPOOLED=""

# Auth.js セッション暗号化キー
AUTH_SECRET=""
